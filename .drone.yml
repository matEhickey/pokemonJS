pipeline:
  test:
    image: node:8.16.0-alpine
    commands:
      - yarn install --ignore-optional
      - yarn test

  publish:
    image: plugins/docker
    registry: http://registryUI:5000
    repo: http://registryUI:5000/pokemonjs
    tags: ['droneCI']
    # auto_tag: true
    when:
      branch:
        - devtools/droneCI

  # frontend:
  #   image: node:6
  #   commands:
  #     - npm install
  #     - npm test
  #
  #
  # notify:
  #   image: plugins/slack
  #   channel: developers
  #   username: drone
