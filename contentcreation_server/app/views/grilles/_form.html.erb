<%= form_with(model: grille, local: true) do |form| %>
  <% if grille.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grille.errors.count, "error") %> prohibited this grille from being saved:</h2>

      <ul>
      <% grille.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
