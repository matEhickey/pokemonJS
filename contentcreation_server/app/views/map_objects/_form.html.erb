<%= form_with(model: map_object, local: true) do |form| %>
  <% if map_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(map_object.errors.count, "error") %> prohibited this map_object from being saved:</h2>

      <ul>
      <% map_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :transparent %>
    <%= form.check_box :transparent %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
