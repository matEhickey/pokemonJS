{
  "name": "PokemonJS",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "(yarn build && sleep 2 && firefox http://localhost:3000) & node_modules/.bin/webpack-dev-server",
    "test": "echo TEST",
    "copy_index": "cp src/index.html dist/index.html",
    "build":      "npx webpack --mode development --devtool source-map  && yarn copy_index",
    "buildProd":  "npx webpack --mode production  && yarn copy_index && rm -r docs && cp -r dist docs"
  },
  "main": "src/gameloop/main.js",
  "devDependencies": {
    "css-loader": "^2.1.1",
    "file-loader": "^3.0.1",
    "style-loader": "^0.23.1",
    "webpack": "^4.30.0",
    "webpack-cli": "^3.3.1",
    "webpack-dev-server": "^3.3.1"
  }
}
